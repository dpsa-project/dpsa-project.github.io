window.SIDEBAR_ITEMS = {"struct":[["Accumulator","Accumulates output shares in memory and eventually flushes accumulations to a datastore. We accumulate output shares into a [`HashMap`] mapping the batch identifier at which the batch interval begins to the accumulated aggregate share, report count and checksum."]]};