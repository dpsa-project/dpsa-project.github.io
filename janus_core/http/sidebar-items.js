window.SIDEBAR_ITEMS = {"fn":[["extract_bearer_token","If the request in `conn` has an `authorization` header, returns the bearer token in the header value. Returns `None` if there is no `authorization` header, and an error if there is an `authorization` header whose value is not a bearer token."],["response_to_problem_details","Turn a [`reqwest::Response`] into a [`HttpApiProblem`]. If applicable, a JSON problem details document is parsed from the request’s body, otherwise it is solely constructed from the response’s status code. (see RFC 7807)"]]};